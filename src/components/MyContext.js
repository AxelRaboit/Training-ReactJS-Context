import React from 'react';

//De cette façon, le composant appelé dans le fichier ProfileData va afficher les datas donné ici par default à savoir Bart au lieu de Lisa
/* const user = {
	name: 'bart',
	age: 9
}

const MyContext = React.createContext(user); */

const MyContext = React.createContext();

export default MyContext